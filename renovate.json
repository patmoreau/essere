{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["local>patmoreau/renovate-config"],
  "rangeStrategy": "bump",
  "minimumReleaseAge": "3 days",
  "constraintsFiltering": "strict",
  "packageRules": [
    {
      "matchPackageNames": ["pnpm"],
      "groupName": "pnpm"
    },
    {
      "description": "typescript-eslint peer range is >=4.8.4 <6.1.0; TS 7 breaks pnpm lint. Lift once typescript-eslint supports TS >=7.1 (typescript-eslint#10940)",
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7.0.0"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["/(^|/)Dockerfile$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+RUN npm install -g pnpm@(?<currentValue>\\S+)"
      ]
    }
  ]
}
